
interface User {
    name: string
    email: string
 }

 
//  let user: User | null = null;
 let user: User={} as User;
 user ={
    name: 'user',
    email: 'user@gmail.com'
 }
//  const friends= ["Arif", "John"] as const;
